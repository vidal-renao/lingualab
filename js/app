
function searchWord(){
const q=document.getElementById("searchBox").value
const r=searchVocab(q)

document.getElementById("result").textContent=JSON.stringify(r,null,2)
}

function addWord(){

const w={
de:document.getElementById("de").value,
es:document.getElementById("es").value,
en:document.getElementById("en").value,
level:document.getElementById("level").value
}


function showWords(){

const words = allWords()

let html=""

words.forEach(w=>{
html+=`<p>${w.de} - ${w.es} (${w.level})</p>`
})

document.getElementById("vocabList").innerHTML=html

}

window.onload=showWords
addUserWord(w)

alert("Word saved locally")
}

